import { Button, Box } from "@chakra-ui/react";

const App = () => {
  return (
    <Box bgColor={"red.200"}>
      <Button bgColor={"blue.500"}>this is a sample mark </Button>
    </Box>
  );
};

export default App;
